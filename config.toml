[panel]
# 面板进度条上限
basic_stat_max = 100
experience_max = 100
level_max = 100

# 面板默认值（配置热更新时会立即应用）
default_stamina = 80
default_satiety = 70
default_thirst = 65
default_mood = 75
default_health = 90
default_affinity = 50
default_experience = 10
default_level = 78

[animation]
# 动画资源根目录（相对/绝对路径都可）
assets_body_root = "assets/body"

# 默认待机动画变体目录
default_happy_idle_variants = [
	"Default/Happy/1",
	"Default/Happy/2",
	"Default/Happy/3",
]

# 启动与拖拽动画目录
startup_root = "StartUP"
raise_dynamic_root = "Raise/Raised_Dynamic"
raise_static_root = "Raise/Raised_Static"

# 关机动画候选目录
shutdown_variants = [
	"Shutdown/2/Happy",
	"Shutdown/Happy_1",
]
